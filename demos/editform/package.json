{
    "name": "editform",
    "version": "1.0.0",
    "description": "All Python application code is in `hello/hello.py`. The backend is called from JavaScript, which is in `hello/web/index.html`.",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "ws": "< 2.0.0",
        "autobahn": ">= 0.12.0"
    }
}
